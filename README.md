# EFCoreDatabaseFirstSample

This project is backend API project using Dot Net Core framework.
It uses the database first approach to create the models using ADO.NET
The poject is set up as using context of a University in which there are multiple Courses and Students and ther relationships with each other.
It also demonstrates the use of lazy, eager and explicit loading.
